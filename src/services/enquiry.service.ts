import { BaseService } from "./base.service";


export class InventoryService extends BaseService {

    public async getInventory(values: any, productSearch: any) {
        try {
           
           
        } catch (error) {
            throw (error)
        }
    }

}